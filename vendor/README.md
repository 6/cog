# vendor/

Reference-only git submodules. These are **not imported** by cog — the agent uses stdlib HTTP directly.

They exist so we can verify our API usage matches the official SDK types and protocol exactly.

- **anthropic-sdk-python** — Anthropic Messages API reference (types, request/response formats, streaming protocol)
